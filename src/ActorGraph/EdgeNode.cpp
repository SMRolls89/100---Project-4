/*
 *
 *
 *
 *
 *
 */

#include "edgeNode.hpp"

//constructor  to initialize an edge between actors costaring in the same movie
edgeNode::edgeNode(movieNode* movie, actorNode* actor1, actorNode* actor2) : edgeNode(movie, actor1, actor2) {}
